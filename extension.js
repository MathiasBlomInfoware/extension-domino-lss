// Minimal entry so Cursor/VS Code activates the extension when a .lss file
// uses language id "lotusscript" (grammar is contributed statically).
exports.activate = function () {};
exports.deactivate = function () {};
